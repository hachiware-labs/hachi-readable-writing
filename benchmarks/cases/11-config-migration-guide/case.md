# Case 11: Configuration migration guide

The document moves operators from version 1 to version 2 of a fictional service. It must distinguish renamed fields from behavior changes, provide before-and-after configuration, sequence the rollout, and preserve a rollback path.

The comparison should check exact mappings, defaults, order of operations, validation behavior, and whether rollback limitations are explicit.

