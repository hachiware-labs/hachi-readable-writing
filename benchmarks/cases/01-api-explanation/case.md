# Case 01: API explanation

The document introduces a fictional transactional email API to application developers who are evaluating an integration. It should explain the request lifecycle, not merely reproduce a field list.

The comparison should check whether all limits and error behaviors survive rewriting; whether the request and response examples agree with the prose; whether English technical terms are retained only where useful in Japanese; and whether headings or bullets support rather than replace the explanation.

## Recorded evaluations

- Fable-5 rated `baseline.ja.md` at 82/100. See `evaluation.fable-5.ja.md`.
